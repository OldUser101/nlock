# Command Line

Currently, nlock doesn't have any command line options to specify. However,
it is planned to have configuration file overrides here in the future.

It would also be nice to specify a log level when running nlock.
